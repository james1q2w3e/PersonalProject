// probably not going to be used, thought I could maybe take in only certain elements into different
// Components, but seems kinda pointless

const bootstrap = require('react-bootstrap')

export default bootstrap;