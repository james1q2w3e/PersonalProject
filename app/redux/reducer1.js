import axios from 'axios';

// CONSTANTS

// ACTION CREATORS

// THUNKY BOIS

// initial state boi
const initialState = {
  // tempArr: [],
  // singleObjectFromArray: {},
}

// reducer
export default function reducer1(state = initialState, action) {

  switch(action.type) {
    default: 
    return state;
  }
}